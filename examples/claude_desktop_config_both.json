{
  "_comment": "Combined config when you want both rba-mcp and its companion abs-mcp running side by side. Drop into ~/Library/Application Support/Claude/claude_desktop_config.json.",
  "mcpServers": {
    "abs": {
      "command": "uvx",
      "args": ["abs-mcp"]
    },
    "rba": {
      "command": "uvx",
      "args": ["rba-mcp"]
    }
  }
}
