{
  "_comment": "Combined config when you want both rba-mcp and its companion abs-mcp running side by side. Drop into ~/Library/Application Support/Claude/claude_desktop_config.json. The --upgrade flag makes uvx check PyPI on each Claude Desktop launch and adopt newer wheels — without it, the long-lived MCP child process stays pinned to whatever was first cached.",
  "mcpServers": {
    "abs": {
      "command": "uvx",
      "args": ["--upgrade", "abs-mcp"]
    },
    "rba": {
      "command": "uvx",
      "args": ["--upgrade", "rba-mcp"]
    }
  }
}
